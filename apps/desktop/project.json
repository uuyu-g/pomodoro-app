{
  "projectType": "application",
  "sourceRoot": "apps/desktop/src",
  "prefix": "app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tauri build"],
        "cwd": "apps/desktop"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tauri dev"],
        "cwd": "apps/desktop"
      }
    }
  },
  "tags": []
}
